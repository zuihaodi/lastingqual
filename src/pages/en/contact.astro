---
import Layout from "../../layouts/Layout.astro";
import Header from "../../components/HeaderEN.astro";
import UnifiedSimplePage from "../../components/UnifiedSimplePage.astro";
import { siteConfig } from "../../config/site";
import { loadCardsByLangPage, loadSimplePageByLang } from "../../lib/content-loader";

const cfg = loadSimplePageByLang("contact", "en");
const cards = loadCardsByLangPage("en", "contact").filter((c) => c.title && c.summary);
---

<Layout title={`${cfg?.heroTitle || "Contact"} | Lasting Qual`} lang="en">
  <Header />
  <UnifiedSimplePage cfg={cfg} cards={cards} lang="en" />
  <footer class="bg-[#002845] text-white py-8">
    <div class="max-w-7xl mx-auto px-4 text-center text-sm">
      <p>&copy; {siteConfig.copyrightYear} {siteConfig.companyName} All rights reserved.</p>
    </div>
  </footer>
</Layout>
